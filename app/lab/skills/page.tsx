import { SkillsLabView } from "@/components/skills/SkillsLabView";

export const metadata = {
  title: "Skills Framework — ForgeOS Lab",
  description: "RC4 Skills Registry, Router, Executor — sandbox mode",
};

export default function SkillsLabPage() {
  return <SkillsLabView />;
}
