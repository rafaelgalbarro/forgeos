# Supabase Preview

When `ENABLE_PREVIEW_SUPABASE_SETUP=true` and `SUPABASE_ACCESS_TOKEN` configured:

- Validate connection
- Sandbox project plan
- Schema, migrations, demo seed
- RLS policies
- Rollback steps

**NO production** database operations.

When API unavailable: manual verifiable plan documented in deployment logs.
