/** ForgeOS Business Skills — Business Accounting permissions (RC4.4). */

import { buildPermissions } from "../shared/factory";
import { ACCOUNTING_DEF } from "./types";

export const ACCOUNTING_PERMISSIONS = buildPermissions(ACCOUNTING_DEF);
