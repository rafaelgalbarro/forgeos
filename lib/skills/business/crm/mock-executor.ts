/** ForgeOS Business Skills — Business CRM mock executor (RC4.4). */

import { buildMockExecutor } from "../shared/factory";
import { CRM_DEF } from "./types";

export const mockExecuteCrm = buildMockExecutor(CRM_DEF);
