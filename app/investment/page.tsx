"use client";

/**
 * ForgeOS Investment — /investment dashboard (Block 6 terminal overview).
 * Legacy cycle UI lives under Strategies / Live flows.
 */

import { InvestmentWorkspace } from "@/components/investment/InvestmentWorkspace";

export default function InvestmentPage() {
  return <InvestmentWorkspace />;
}
