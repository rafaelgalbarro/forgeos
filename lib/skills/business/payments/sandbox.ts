/** ForgeOS Business Skills — Business Payments sandbox (RC4.4). */

import { buildSandbox } from "../shared/factory";
import { PAYMENTS_DEF } from "./types";

export const PAYMENTS_SANDBOX = buildSandbox(PAYMENTS_DEF);
