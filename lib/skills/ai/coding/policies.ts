/** ForgeOS AI coding capability — policies (RC4.7). */

import { CODING_CONFIG } from "../shared/capabilities";
import { buildPolicies } from "../shared/capability-factory";

export const policies = buildPolicies(CODING_CONFIG);
