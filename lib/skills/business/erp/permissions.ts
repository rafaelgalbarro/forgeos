/** ForgeOS Business Skills — Business ERP permissions (RC4.4). */

import { buildPermissions } from "../shared/factory";
import { ERP_DEF } from "./types";

export const ERP_PERMISSIONS = buildPermissions(ERP_DEF);
