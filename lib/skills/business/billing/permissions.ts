/** ForgeOS Business Skills — Business Billing permissions (RC4.4). */

import { buildPermissions } from "../shared/factory";
import { BILLING_DEF } from "./types";

export const BILLING_PERMISSIONS = buildPermissions(BILLING_DEF);
