import { BackendFactoryLab } from "@/components/lab/BackendFactoryLab";

export const metadata = {
  title: "Backend Factory — ForgeOS",
  description: "ForgeOS lab console: backend blueprint factory (Epic 6.4)",
};

export default function BackendFactoryLabPage() {
  return <BackendFactoryLab />;
}
