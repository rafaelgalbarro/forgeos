/** Analytics Dashboards — sandbox config (RC4.6). */

import { buildSandbox } from "../shared/provider-kit";
import { DASHBOARDS_DEF } from "./types";

export const DASHBOARDS_SANDBOX = buildSandbox(DASHBOARDS_DEF);
