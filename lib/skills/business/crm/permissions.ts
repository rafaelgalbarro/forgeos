/** ForgeOS Business Skills — Business CRM permissions (RC4.4). */

import { buildPermissions } from "../shared/factory";
import { CRM_DEF } from "./types";

export const CRM_PERMISSIONS = buildPermissions(CRM_DEF);
