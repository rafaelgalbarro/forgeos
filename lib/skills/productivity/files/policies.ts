/** ForgeOS Productivity Files — policies (RC4.3). */

import { createProductivityPolicy } from "../create-provider";
import { FILES_CONFIG } from "../provider-configs";

export const FILES_POLICY = createProductivityPolicy(FILES_CONFIG);
