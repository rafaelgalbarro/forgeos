import { RuntimeObservabilityLab } from "@/components/lab/RuntimeObservabilityLab";

export const metadata = {
  title: "Runtime Observability — ForgeOS",
  description: "ForgeOS lab console: runtime observability and self-healing RC1 (Epic 4.6)",
};

export default function RuntimeObservabilityLabPage() {
  return <RuntimeObservabilityLab />;
}
