import { NetworkLabView } from "@/components/network/NetworkLabView";

export const metadata = {
  title: "ForgeOS Network — Lab",
  description: "RC10 Network lab harness — consent, privacy, anonymization",
};

export default function NetworkLabPage() {
  return <NetworkLabView />;
}
