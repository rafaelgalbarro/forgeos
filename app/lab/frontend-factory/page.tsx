import { FrontendFactoryLab } from "@/components/lab/FrontendFactoryLab";

export const metadata = {
  title: "Frontend Factory — ForgeOS",
  description: "ForgeOS lab console: frontend blueprint factory (Epic 6.3)",
};

export default function FrontendFactoryLabPage() {
  return <FrontendFactoryLab />;
}
