/** ForgeOS Business Skills — Business Accounting sandbox (RC4.4). */

import { buildSandbox } from "../shared/factory";
import { ACCOUNTING_DEF } from "./types";

export const ACCOUNTING_SANDBOX = buildSandbox(ACCOUNTING_DEF);
