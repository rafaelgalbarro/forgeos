import { WorkersLab } from "@/components/lab/WorkersLab";

export const metadata = {
  title: "Worker Runtime — ForgeOS",
  description: "ForgeOS lab console: official department workers and mock execution (Epic 4.3)",
};

export default function WorkersLabPage() {
  return <WorkersLab />;
}
