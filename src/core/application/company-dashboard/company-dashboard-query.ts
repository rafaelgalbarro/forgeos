/**
 * PROGRAM 6090 — Server-side company dashboard query entry.
 * Import only from Server Components / server routes.
 */

export { buildCompanyDashboardReadModel as getCompanyDashboard } from "./query-handler";
export { buildCompanyDashboardReadModel } from "./query-handler";
