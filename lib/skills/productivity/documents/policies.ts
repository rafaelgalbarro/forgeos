/** ForgeOS Productivity Documents — policies (RC4.3). */

import { createProductivityPolicy } from "../create-provider";
import { DOCUMENTS_CONFIG } from "../provider-configs";

export const DOCUMENTS_POLICY = createProductivityPolicy(DOCUMENTS_CONFIG);
