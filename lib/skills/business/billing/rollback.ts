/** ForgeOS Business Skills — Business Billing rollback (RC4.4). */

import { buildRollback } from "../shared/factory";
import { BILLING_DEF } from "./types";

export const BILLING_ROLLBACK = buildRollback(BILLING_DEF);
