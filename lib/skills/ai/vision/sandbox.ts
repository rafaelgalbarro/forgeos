/** ForgeOS AI vision capability — sandbox (RC4.7). */

import { VISION_CONFIG } from "../shared/capabilities";
import { buildSandboxConfig } from "../shared/capability-factory";

export const sandbox = buildSandboxConfig(VISION_CONFIG);
