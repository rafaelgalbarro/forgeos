import { TaskQueueLab } from "@/components/lab/TaskQueueLab";

export const metadata = {
  title: "Task Queue — ForgeOS",
  description: "ForgeOS lab console: official runtime task queue (Epic 4.4)",
};

export default function TaskQueueLabPage() {
  return <TaskQueueLab />;
}
