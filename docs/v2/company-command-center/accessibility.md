# Accessibility

The page uses semantic headings and keyboard-accessible controls:

- mode toggles are buttons
- filter is label-associated select
- links remain keyboard focusable
- status badges preserve text labels (not color-only)

Mobile/tablet/desktop grid behavior is handled by responsive CSS breakpoints.
