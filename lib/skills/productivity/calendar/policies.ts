/** ForgeOS Productivity Calendar — policies (RC4.3). */

import { createProductivityPolicy } from "../create-provider";
import { CALENDAR_CONFIG } from "../provider-configs";

export const CALENDAR_POLICY = createProductivityPolicy(CALENDAR_CONFIG);
