# Certification Evidence

Command: `node scripts/run-v2-certification.js` (also `--skip-build` after sequential pipeline)

Output: `artifacts/v2-certification/latest.json` and `docs/architecture-v2/certification/certification-results.json`

Latest declaration:

```
FORGEOS V2 — END-TO-END CERTIFIED
status=CERTIFIED
```

Includes live ATLAS integration checks + lineage + composition file presence. No hardcoded PASS.
