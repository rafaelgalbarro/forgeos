/** ForgeOS Business Skills — Business ERP sandbox (RC4.4). */

import { buildSandbox } from "../shared/factory";
import { ERP_DEF } from "./types";

export const ERP_SANDBOX = buildSandbox(ERP_DEF);
