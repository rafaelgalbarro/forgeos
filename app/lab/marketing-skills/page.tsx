import { MarketingSkillsLabView } from "@/components/skills/MarketingSkillsLabView";

export const metadata = {
  title: "Marketing Skills — ForgeOS Lab",
  description: "RC4.5 Marketing Skills — campaigns, SEO, ads, email (sandbox mock)",
};

export default function MarketingSkillsLabPage() {
  return <MarketingSkillsLabView />;
}
