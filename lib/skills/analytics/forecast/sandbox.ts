/** Analytics Forecast — sandbox config (RC4.6). */

import { buildSandbox } from "../shared/provider-kit";
import { FORECAST_DEF } from "./types";

export const FORECAST_SANDBOX = buildSandbox(FORECAST_DEF);
