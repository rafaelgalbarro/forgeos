/** Analytics Reports — sandbox config (RC4.6). */

import { buildSandbox } from "../shared/provider-kit";
import { REPORTS_DEF } from "./types";

export const REPORTS_SANDBOX = buildSandbox(REPORTS_DEF);
