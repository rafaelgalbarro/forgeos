/** ForgeOS Business Skills — Business ERP rollback (RC4.4). */

import { buildRollback } from "../shared/factory";
import { ERP_DEF } from "./types";

export const ERP_ROLLBACK = buildRollback(ERP_DEF);
