AI routing uses existing ForgeOS runtime:

- Capability Registry (`lib/capabilities/capability-registry.ts`)
- AI Runtime (`lib/ai-runtime`)

Per-generation routing metadata recorded:

- provider
- model
- promptVersion
- tokenUsageEstimate
- estimatedCost
- latencyMs
- outputValidation
