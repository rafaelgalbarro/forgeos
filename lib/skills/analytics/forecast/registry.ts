/** Analytics Forecast — registry (RC4.6). */

import { buildRegistry } from "../shared/provider-kit";
import { FORECAST_DEF } from "./types";

export const FORECAST_REGISTRY = buildRegistry(FORECAST_DEF);
