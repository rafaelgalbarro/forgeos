/** ForgeOS Business Skills — Business Customers sandbox (RC4.4). */

import { buildSandbox } from "../shared/factory";
import { CUSTOMERS_DEF } from "./types";

export const CUSTOMERS_SANDBOX = buildSandbox(CUSTOMERS_DEF);
