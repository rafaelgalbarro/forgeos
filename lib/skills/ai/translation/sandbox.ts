/** ForgeOS AI translation capability — sandbox (RC4.7). */

import { TRANSLATION_CONFIG } from "../shared/capabilities";
import { buildSandboxConfig } from "../shared/capability-factory";

export const sandbox = buildSandboxConfig(TRANSLATION_CONFIG);
