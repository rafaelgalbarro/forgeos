/** ForgeOS Business Skills — Business CRM policies (RC4.4). */

import { buildPolicies } from "../shared/factory";
import { CRM_DEF } from "./types";

export const CRM_POLICIES = buildPolicies(CRM_DEF);
