/** Venture Intelligence — program-specific types (scaffold). */

export type VentureIntelligenceModuleId =
  | "intelligence-layer"
  | "ceo"
  | "board"
  | "fos";

export type IntelligenceConnectionStatus = "connected" | "disconnected" | "scaffold";
