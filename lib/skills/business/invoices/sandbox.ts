/** ForgeOS Business Skills — Business Invoices sandbox (RC4.4). */

import { buildSandbox } from "../shared/factory";
import { INVOICES_DEF } from "./types";

export const INVOICES_SANDBOX = buildSandbox(INVOICES_DEF);
