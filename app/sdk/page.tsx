import { SdkView } from "@/components/ecosystem/SdkView";

export const metadata = {
  title: "SDK — ForgeOS",
  description: "RC9 ForgeOS SDK — developer surface for ecosystem extensions",
};

export default function SdkPage() {
  return <SdkView />;
}
