import { DatabaseFactoryLab } from "@/components/lab/DatabaseFactoryLab";

export const metadata = {
  title: "Database Factory — ForgeOS",
  description: "ForgeOS lab console: database blueprint factory (Epic 6.5)",
};

export default function DatabaseFactoryLabPage() {
  return <DatabaseFactoryLab />;
}
