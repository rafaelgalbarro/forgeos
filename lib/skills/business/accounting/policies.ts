/** ForgeOS Business Skills — Business Accounting policies (RC4.4). */

import { buildPolicies } from "../shared/factory";
import { ACCOUNTING_DEF } from "./types";

export const ACCOUNTING_POLICIES = buildPolicies(ACCOUNTING_DEF);
