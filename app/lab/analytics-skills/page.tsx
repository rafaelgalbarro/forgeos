import { AnalyticsSkillsLabView } from "@/components/skills/AnalyticsSkillsLabView";

export const metadata = {
  title: "Analytics Skills — ForgeOS Lab",
  description: "RC4.6 Analytics Skills — dashboards, reports, KPIs, forecast (sandbox mock)",
};

export default function AnalyticsSkillsLabPage() {
  return <AnalyticsSkillsLabView />;
}
