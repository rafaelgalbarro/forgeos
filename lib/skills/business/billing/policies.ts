/** ForgeOS Business Skills — Business Billing policies (RC4.4). */

import { buildPolicies } from "../shared/factory";
import { BILLING_DEF } from "./types";

export const BILLING_POLICIES = buildPolicies(BILLING_DEF);
