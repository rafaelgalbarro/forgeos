import { ExecutiveRuntimeLab } from "@/components/lab/ExecutiveRuntimeLab";

export const metadata = {
  title: "Mission Control — ForgeOS",
  description: "ForgeOS engineering console: observa CEO + Board + Consensus + Decision Graph (Epic 3.2.2)",
};

export default function ExecutiveRuntimeLabPage() {
  return <ExecutiveRuntimeLab />;
}
