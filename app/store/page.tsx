import { EcosystemStoreView } from "@/components/marketplace/EcosystemStoreView";

export const metadata = {
  title: "Skill Store — ForgeOS",
  description: "RC4.8 Official Skill Store + RC9 Ecosystem packs (sandbox)",
};

export default function StorePage() {
  return <EcosystemStoreView />;
}
