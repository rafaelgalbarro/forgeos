import { EnterpriseLabView } from "@/components/enterprise/EnterpriseLabView";

export const metadata = {
  title: "Enterprise Lab — ForgeOS",
  description: "RC11 — Enterprise lab engineering harness",
};

export default function EnterpriseLabPage() {
  return <EnterpriseLabView />;
}
