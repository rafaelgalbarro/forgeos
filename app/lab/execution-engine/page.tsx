import { ExecutionEngineLab } from "@/components/lab/ExecutionEngineLab";

export const metadata = {
  title: "Execution Engine — ForgeOS",
  description: "ForgeOS lab console: runtime execution engine RC1 (Epic 4.5)",
};

export default function ExecutionEngineLabPage() {
  return <ExecutionEngineLab />;
}
