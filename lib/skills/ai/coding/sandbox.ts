/** ForgeOS AI coding capability — sandbox (RC4.7). */

import { CODING_CONFIG } from "../shared/capabilities";
import { buildSandboxConfig } from "../shared/capability-factory";

export const sandbox = buildSandboxConfig(CODING_CONFIG);
