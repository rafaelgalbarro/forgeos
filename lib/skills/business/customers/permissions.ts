/** ForgeOS Business Skills — Business Customers permissions (RC4.4). */

import { buildPermissions } from "../shared/factory";
import { CUSTOMERS_DEF } from "./types";

export const CUSTOMERS_PERMISSIONS = buildPermissions(CUSTOMERS_DEF);
