/** ForgeOS Business Skills — Business Customers rollback (RC4.4). */

import { buildRollback } from "../shared/factory";
import { CUSTOMERS_DEF } from "./types";

export const CUSTOMERS_ROLLBACK = buildRollback(CUSTOMERS_DEF);
