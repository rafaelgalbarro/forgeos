/** ForgeOS AI translation capability — permissions (RC4.7). */

import { TRANSLATION_CONFIG } from "../shared/capabilities";
import { buildPermissions } from "../shared/capability-factory";

export const permissions = buildPermissions(TRANSLATION_CONFIG);
