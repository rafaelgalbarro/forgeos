/** ForgeOS Business Skills — Business CRM rollback (RC4.4). */

import { buildRollback } from "../shared/factory";
import { CRM_DEF } from "./types";

export const CRM_ROLLBACK = buildRollback(CRM_DEF);
