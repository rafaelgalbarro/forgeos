/** ForgeOS Business Skills — Business Accounting rollback (RC4.4). */

import { buildRollback } from "../shared/factory";
import { ACCOUNTING_DEF } from "./types";

export const ACCOUNTING_ROLLBACK = buildRollback(ACCOUNTING_DEF);
