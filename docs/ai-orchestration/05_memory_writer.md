# Memory Writer

Each execution stored in `forgeos-ai-orchestration-executions`:

- taskId, provider, model, timestamp, latencyMs
- fallbackUsed, warnings, costEstimate
- inputSize, outputSize, ventureId

No API keys. No sensitive payloads.
