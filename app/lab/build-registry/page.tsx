import { BuildRegistryLab } from "@/components/lab/BuildRegistryLab";

export const metadata = {
  title: "Build Registry — ForgeOS",
  description: "ForgeOS lab console: official build registry for generators, providers, artifacts, workers, templates, and stacks (Epic 6.2)",
};

export default function BuildRegistryLabPage() {
  return <BuildRegistryLab />;
}
