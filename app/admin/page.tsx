import { AdminConsoleView } from "@/components/enterprise/AdminConsoleView";

export const metadata = {
  title: "Admin Console — ForgeOS",
  description: "RC11 — Enterprise admin console demo",
};

export default function AdminPage() {
  return <AdminConsoleView />;
}
