/** @deprecated Prefer ./kernel-commands — kept for import compatibility. */
export type {
  ApplicationCommandName,
  ApplicationCommand,
  CommandResult,
  KernelCommandName,
} from "./kernel-commands";
