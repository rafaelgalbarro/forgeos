/** ForgeOS AI translation capability — registry (RC4.7). */

import { TRANSLATION_CONFIG } from "../shared/capabilities";
import { buildRegistry } from "../shared/capability-factory";

export const registry = buildRegistry(TRANSLATION_CONFIG);
