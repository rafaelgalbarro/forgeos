/** ForgeOS Business Skills — Business Contracts rollback (RC4.4). */

import { buildRollback } from "../shared/factory";
import { CONTRACTS_DEF } from "./types";

export const CONTRACTS_ROLLBACK = buildRollback(CONTRACTS_DEF);
