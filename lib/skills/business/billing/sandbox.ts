/** ForgeOS Business Skills — Business Billing sandbox (RC4.4). */

import { buildSandbox } from "../shared/factory";
import { BILLING_DEF } from "./types";

export const BILLING_SANDBOX = buildSandbox(BILLING_DEF);
