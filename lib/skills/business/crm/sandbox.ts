/** ForgeOS Business Skills — Business CRM sandbox (RC4.4). */

import { buildSandbox } from "../shared/factory";
import { CRM_DEF } from "./types";

export const CRM_SANDBOX = buildSandbox(CRM_DEF);
