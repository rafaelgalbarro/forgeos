/** ForgeOS Productivity Files — permissions (RC4.3). */

import { createProductivityPermissions } from "../create-provider";
import { FILES_CONFIG } from "../provider-configs";

export const FILES_PERMISSIONS = createProductivityPermissions(FILES_CONFIG);
