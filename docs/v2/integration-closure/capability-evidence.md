# Capability Evidence

Capabilities exercised via delivery registration + application PlanOutput/GenerateOutput (fixture-driven titles; **not** hardcoded into engines):

Venture, Brand, Product, Website, Web Application, Backend, Database, API, QA → artifacts/outputs with checksums and contentRef.

Code Generation → CanonicalCodebase with file/package manifests.

Static Validation recorded; sandbox explicitly `SANDBOX_UNAVAILABLE`.

Release + Preview Deployment Plan via delivery kernel (dry-run).
