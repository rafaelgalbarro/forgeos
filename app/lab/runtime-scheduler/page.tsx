import { RuntimeSchedulerLab } from "@/components/lab/RuntimeSchedulerLab";

export const metadata = {
  title: "Runtime Scheduler — ForgeOS",
  description: "ForgeOS lab console: plan runtime tasks from Event Bus signals (Epic 4.1)",
};

export default function RuntimeSchedulerLabPage() {
  return <RuntimeSchedulerLab />;
}
