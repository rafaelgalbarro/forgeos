import { StateMachineLab } from "@/components/lab/StateMachineLab";

export const metadata = {
  title: "Venture State Machine — ForgeOS",
  description: "ForgeOS lab console: official venture lifecycle state machine (Epic 4.2)",
};

export default function StateMachineLabPage() {
  return <StateMachineLab />;
}
