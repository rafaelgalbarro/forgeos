# Export

## ZIP (`code-zip-exporter.ts`)

Uses `jszip` to produce valid ZIP archives in browser and Node.

## Manifest (`code-manifest-exporter.ts`)

JSON manifest + Markdown report with file list, checksums, validation status, warnings.

No real git push — export only.
