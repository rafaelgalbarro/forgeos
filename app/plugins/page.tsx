import { PluginsView } from "@/components/ecosystem/PluginsView";

export const metadata = {
  title: "Plugins — ForgeOS",
  description: "RC9 ForgeOS Plugins — sandbox extension catalog",
};

export default function PluginsPage() {
  return <PluginsView />;
}
