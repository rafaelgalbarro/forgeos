/** Analytics Reports — registry (RC4.6). */

import { buildRegistry } from "../shared/provider-kit";
import { REPORTS_DEF } from "./types";

export const REPORTS_REGISTRY = buildRegistry(REPORTS_DEF);
