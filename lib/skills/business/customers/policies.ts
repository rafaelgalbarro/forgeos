/** ForgeOS Business Skills — Business Customers policies (RC4.4). */

import { buildPolicies } from "../shared/factory";
import { CUSTOMERS_DEF } from "./types";

export const CUSTOMERS_POLICIES = buildPolicies(CUSTOMERS_DEF);
