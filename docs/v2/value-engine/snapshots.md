# Value Snapshots

Immutable `ValueSnapshot` records:

- stage, dimensions, metrics, evidence
- economics, risks
- next milestone, recommendation
- confidence, timestamp

Used for evolution comparison (`CreateValueSnapshot`). Prior snapshots are not mutated when new evidence arrives.
